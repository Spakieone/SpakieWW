# SpakieWW

Установка:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Spakieone/SpakieWW/main/install.sh)
```

Запуск:

```bash
spakie
```

Удаление:

```bash
sudo rm -rf /opt/spakieww /usr/local/bin/spakie
```
